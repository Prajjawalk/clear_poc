"""AppConfig for the location app."""

from django.apps import AppConfig


class LocationConfig(AppConfig):
    """Configuration for the location app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "location"
